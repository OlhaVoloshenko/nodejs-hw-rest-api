const { ContactModel, schemasContact } = require("./ContactModel");
const { UserModel, schemasUser } = require("./UserModel");

module.exports = {
  ContactModel,
  schemasContact,
  UserModel,
  schemasUser,
};
